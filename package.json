{
  "name": "cancerinfo-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "CancerInfo API - Free, structured, global clinical oncology REST API with fact-level provenance",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "build": "python3 -c \"from app.main import app; print('CancerInfo FastAPI verified successfully')\"",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "test": "pytest tests/ -v",
    "lint": "python3 -m pytest tests/ -q"
  }
}
