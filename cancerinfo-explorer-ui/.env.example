# Optional: Override the base URL of the CancerInfo API backend
# In local development with Vite dev server proxy, leave empty to proxy /v1 to http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000
